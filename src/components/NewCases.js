import React from 'react';
import LineChart from './Chart';

const NewCases = () => {
  const data = 'data';
  return (
    <div>
      <h3>{data}</h3>
      <LineChart />
    </div>
  );
};

export default NewCases;
